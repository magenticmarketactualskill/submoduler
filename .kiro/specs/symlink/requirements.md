Submoduler init creates script in SubmoduleParent bin/generate_parent_symlink.rb that creates symlink folder named ./submodule_children to SubmoduleParent folder

Submoduler init creates script in SubmoduleChild bin/generate_child_symlinks.rb that creates symlink folder named ./submodule_children to SubmoduleChild folders
 